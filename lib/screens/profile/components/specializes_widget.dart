import 'package:flutter/material.dart';

class SpecializesWidget extends StatelessWidget {
  const SpecializesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
